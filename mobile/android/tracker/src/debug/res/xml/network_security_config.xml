<?xml version="1.0" encoding="utf-8"?>
<!-- Debug build only: plain HTTP to any host, so a phone can post to an API on
     your PC's LAN address (e.g. http://192.168.1.20:4000). Release stays HTTPS. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
