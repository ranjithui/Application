import CircularsPage from './CircularsPage';

/** Operations view of circulars (registered by the wireframe in parents.js as `op-circulars`). */
export default function OpCircularsPage() {
  return <CircularsPage title="Circulars" sub="Internal and parent-facing circulars, with acknowledgement tracked." />;
}
